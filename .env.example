# Sab optional hai — app bina kisi env var ke live API par chalti hai
# (https://api.rewebtech.in/api/v1, dekho lib/apiBase.js).
#
# Yeh sirf tab chahiye jab API apni machine par chala rahe ho. Dhyan rakhna:
# tab login localhost par hi kaam karega, prod API par nahi — session cookie
# SameSite=Lax aur .rewebtech.in par scoped hai.
# NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
